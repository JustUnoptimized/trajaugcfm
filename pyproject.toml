[project]
name = "trajaugcfm"
version = "0.0.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
extend-include = ["src/**/*.py", "scripts/**/*.py"]

[tool.ruff.lint]
ignore = ["F722"]
